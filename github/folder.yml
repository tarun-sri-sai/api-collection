info:
  name: github
  type: folder
  seq: 1

request:
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{GITHUB_TOKEN}}"
