info:
  name: list-everything
  type: http
  seq: 1

http:
  method: POST
  url: https://api.notion.com/v1/search
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
