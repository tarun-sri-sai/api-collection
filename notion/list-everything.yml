info:
  name: list-everything
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
