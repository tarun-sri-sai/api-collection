info:
  name: query-watched-movies
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/databases/{{DATABASE_ID}}/query?filter_properties=title"
  params:
    - name: filter_properties
      value: title
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
